{% extends 'base.html.twig' %}

{% block title %}ValidationEpreuve{% endblock %}

{% block body %}
    <h1>ValidationEpreuve</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ validation_epreuve.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ validation_epreuve.date ? validation_epreuve.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Resultat</th>
                <td>{{ validation_epreuve.resultat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('validation_epreuve_index') }}">back to list</a>

    <a href="{{ path('validation_epreuve_edit', {'id': validation_epreuve.id}) }}">edit</a>

    {{ include('validation_epreuve/_delete_form.html.twig') }}
{% endblock %}
