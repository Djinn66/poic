{% extends 'base.html.twig' %}

{% block title %}Edit Epreuve{% endblock %}

{% block body %}
    {{ include('epreuve/_form.html.twig', { type:'Editer','button_label': 'Update'}) }}

    <a href="{{ path('epreuve_index') }}">back to list</a>
{% if editMode  %}
    {{ include('epreuve/_delete_form.html.twig') }}
{% endif %}
{% endblock %}
