{% extends 'base.html.twig' %}

{% block title %}{{ editMode ? "Edit" : "New"  }} Epreuve{% endblock %}

{% block body %}
    {% if editMode  %}
        {% set type = 'Editer' %}
    {% else %}
        {% set type = 'Créer' %}
    {% endif %}
    {{ include('epreuve/_form.html.twig', { type: type,'button_label': 'Update'}) }}

    <a href="{{ path('epreuve_index') }}">back to list</a>

{% endblock %}
{% block javascripts %}
    {{ encore_entry_script_tags('bareme') }}
{% endblock %}
