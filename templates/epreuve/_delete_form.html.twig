<form method="post" action="{{ path('epreuve_delete', {'id': e.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ e.id) }}">
    <button class="btn btn-outline-dark btn-sm" title="Supprimer">x</button>
</form>
